# Direct dependencies only, with lower bounds rather than exact pins.
#
# This file used to be a full `pip freeze` from Windows + Python 3.14 - ~50
# packages pinned to the exact build resolved there, including transitive
# ones. That reproduces this machine but is hostile to deploying anywhere
# else: a host on a different OS or Python has no matching wheel for some
# of those pins, the install fails, and the app dies at import with a
# redacted ModuleNotFoundError that points at our code rather than at the
# dependency that never installed.
#
# The exact freeze is kept in requirements-lock.txt if you need to
# reproduce this machine's environment precisely.
openpyxl>=3.1
pandas>=2.0
pydantic>=2.0
PyYAML>=6.0
rapidfuzz>=3.0
streamlit>=1.30

# Tests only - harmless to install alongside.
pytest>=8.0
